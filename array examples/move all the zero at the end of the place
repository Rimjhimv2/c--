vector<int> moveZeros(int n, vector<int> a) {
    // step - 1
    vector<int> temp;    
    for(int i = 0; i < n; i++) {
        if(a[i] != 0) {
            temp.push_back(a[i]);
        }
    }

    // step - 2
    int nz = temp.size();     
    for(int i = 0; i < nz; i++) {
        a[i] = temp[i];
    }

    // step - 3
    for(int i = nz; i < n; i++) {
        a[i] = 0;
    }

    return a;
}
//ye brutrforce code hai 
//sbse phle non zero element ko temp m store kardo and then temp ka size dekho then starting of array se lekar temp k size tak non zero element daldo then baki jo bachi hai array space usme zero daldo ye hai main crucks of this bruteforce code 

//time complexity=
Time Complexity Analysis

Step 1 (for loop over n elements)

हर element को check किया जा रहा है → O(n)

Step 2 (copy temp to a)

temp की size nz ≤ n है

Worst case (अगर array में zero ही नहीं है) → nz = n

Complexity = O(n)

Step 3 (fill 0’s in remaining positions)

Worst case → फिर से O(n)

//now space complexity 
यहाँ एक नया vector temp बनाया गया है।

Worst case में, अगर array में कोई भी 0 नहीं है, तो temp में सारे n elements चले जाएंगे।

👉 यानी temp की size O(n) हो सकती है।

बाकी variables (जैसे nz, i) तो सिर्फ integers हैं, जिनका space negligible है → O(1).
so here spce complexity O(1) hai.











//now lets move to its optimal solution 
j = -1;
for (i = 0; i < n; i++) {
    if (arr[i] == 0) {// ham yaha par ist zro ka index dhund rahe hai  
        j = i;  //and then us index ko j m  store kar rahe 
        break;
    }
}
//yaha par eski Time Complexity of this step is: O(n) in worst case.
//ab ham non zero element ko dhundenge 

//Step 2: Start swapping non-zero elements with zero (from index j onwards)
for (i = j + 1; i < n; i++) {
    if (arr[i] != 0) {
        swap(arr[i], arr[j]);
        j++;
    }
}
//tart from i = j+1 (just after the first zero):
//You're scanning the rest of the array for non-zero elements.
//If arr[i] != 0:
//That means you’ve found a non-zero element after a zero.

//So, swap it with the element at index j (which is zero).
//Then increment j to the next zero position.
//If arr[i] == 0, do nothing and keep moving i forward.

//exmaple arr = {1, 0, 2, 3, 2, 0, 0, 4, 5, 1}

Time Complexity:

First loop (finding first zero): O(n)

Second loop (swapping non-zeros): O(n)

🟢 Total: O(n)
🟢 Space: O(1) (in-place)

✅ Summary:

This is the optimal solution because:

Only one pass to find the first zero

Then only one more pass to swap non-zeros

Maintains the relative order of non-zero elements

Moves all 0s to the end


//now lets take a proper full code of optimal solution 
vector<int> moveZeros(int n,vector<int> a){
int j=-1;
 for(int i=0;i<=n;i++){
    if(arr[i]==0){
    j=i;
    break;
    }
 }
  if(j==-1)
  return a;

  for(int i=j+1;i<=n;i++){
    if(arr[i]!=0){
        swap(a[i],a[j]);
        j++;
    }
  }
  return a;
}